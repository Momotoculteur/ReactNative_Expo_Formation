import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonStyle: {
        borderRadius: 20,
        padding: 10,
        backgroundColor: 'red'
    },
    buttonDelete: {
        borderRadius: 20,
        padding: 10,
        backgroundColor: 'gray'
    }
});